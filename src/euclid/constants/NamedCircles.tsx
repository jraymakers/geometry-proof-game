import { circle } from '../../E/functions/VariableMakers';

export const circleAlpha = circle('α');
export const circleBeta = circle('β');

export const α = circle('α');
export const β = circle('β');

export const Circles = {
  α: circle('α'),
  β: circle('β'),
};


export const Cr = {
  α: circle('α'),
  β: circle('β'),
};
